
lenh chay docker compose: docker compose -f docker-compose.infra.yml up -d
link ui swagger: https://editor.swagger.io/

Đặt lại password cho user kibana_system( chạy trong container els): elasticsearch-reset-password -u kibana_system -i
# Nhập password mới khi được hỏi: kibana123
-> vào ui kibana: login với els: elastic - elastic123

build avro: mvn clean compile

Subtitle
cd tools\subtitle-worker
pip install -r requirements.txt
python app.py

- Khoi dong lai winnat(giai phong port)- khi livekit bi trung port
net stop winnat
net start winnat